name: Deploy Portfolio to S3

on:
  push:
    branches: [ main ]
    paths:
      - 'index.html'
      - 'assets/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    concurrency:
      group: deploy-portfolio
      cancel-in-progress: true

    env:
      S3_BUCKET: chadm-portfolio
      CF_DISTRIBUTION_ID: E3DGQMN9TWSU05

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::376374282414:role/GitHubActions-Exigency-Deploy
          aws-region: us-east-2

      - name: Deploy site files to S3
        run: |
          aws s3 cp index.html s3://${{ env.S3_BUCKET }}/index.html \
            --cache-control "public, max-age=60, must-revalidate" \
            --content-type "text/html; charset=utf-8"
          aws s3 cp assets/chainly-screenshot.png s3://${{ env.S3_BUCKET }}/assets/chainly-screenshot.png \
            --cache-control "public, max-age=86400" \
            --content-type "image/png"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CF_DISTRIBUTION_ID }} \
            --paths "/index.html" "/" "/assets/chainly-screenshot.png"

      - name: Deployed
        run: echo "✅ Deployed https://chadmurchison.com/ (index.html + Chainly screenshot only)"
